// test/example.test.js
const assert = require('chai').assert; // O usa require('assert') si prefieres sin Chai
describe('Test de ejemplo', () => {
    it('should return true', () => {
        assert.isTrue(true);
    });
});